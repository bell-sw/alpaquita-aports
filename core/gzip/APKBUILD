# Description: These tools allow you to compress/uncompress .gz files. Also
# gunzip can uncompress .Z files created by compress utility.

pkgname=gzip
pkgver=1.14
pkgrel=1
pkgdesc="Data compression program"
subpackages="$pkgname-doc $pkgname-dbg"
url="https://www.gnu.org/software/gzip/"
arch="all"
license="GPL-3.0-or-later"
# we build fmv only on x86_64 for now
[ "$CARCH" = 'x86_64' ] && makedepends="fmv"
depends="less"
checkdepends="diffutils grep perl"
source="https://ftp.gnu.org/gnu/gzip/gzip-$pkgver.tar.xz
	0100-CVE-2026-41992.patch
"
fmv_targets="x86-64-v3 default"
fmv_check_files="gzip"
fmv_functions="
	inflate.c:huft_build
	inflate.c:gzip_inflate
	inflate.c:inflate_codes
	inflate.c:inflate_stored
	inflate.c:inflate_dynamic
	inflate.c:inflate_block
	util.c:flush_window
"

# secfixes:
#   1.14-r1:
#     - CVE-2026-41992
#   1.12-r0:
#     - CVE-2022-1271

build() {
	optimize_gcc_build

	# avoid text relocation
	export DEFS="NO_ASM"

	./configure \
		--build=$CBUILD \
		--host=$CHOST \
		--prefix=/usr \
		--mandir=/usr/share/man \
		--infodir=/usr/share/info
	make V=1
}

check() {
	make check
}

package() {
	make DESTDIR=$pkgdir install

	rm -rf "$pkgdir"/usr/lib/charset.alias
	rmdir -p "$pkgdir"/usr/lib 2>/dev/null || true

	ln -sf gunzip "$pkgdir"/usr/bin/uncompress
}

sha512sums="
82aef53188b3e69b51b7ddab5b8c44a11a5b73c0039b22a315a0c7d244694feab0146748add4265901eb1b4c0cee8a9eb69594995f098830d964091af97079c5  gzip-1.14.tar.xz
dbe2af1cc3bf7ce5794e5beef799f1ad834d408d5129b25dd5b1625e16058673f868365f9f6e0bbc1b07538b5c51841e77196e1059073e1b5ab302766efa0748  0100-CVE-2026-41992.patch
"
