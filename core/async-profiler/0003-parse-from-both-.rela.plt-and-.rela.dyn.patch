From 7e4f8c8322574f301cf15d0b63a8e23a7ba77f63 Mon Sep 17 00:00:00 2001
From: Takha Polat <takha.polat@bell-sw.com>
Date: Mon, 2 Dec 2024 16:47:09 +0000
Subject: [PATCH] fix
 https://github.com/async-profiler/async-profiler/issues/1060

---
 src/symbols_linux.cpp | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/src/symbols_linux.cpp b/src/symbols_linux.cpp
index 3e15da0..62fcda7 100644
--- a/src/symbols_linux.cpp
+++ b/src/symbols_linux.cpp
@@ -317,7 +317,8 @@ void ElfParser::parseDynamicSection() {
             if (pltrelsz != 0 && got_start != NULL) {
                 // The number of entries in .got.plt section matches the number of entries in .rela.plt
                 _cc->setGlobalOffsetTable(got_start, got_start + pltrelsz / relent, false);
-            } else if (rel != NULL && relsz != 0) {
+            }
+	    if (rel != NULL && relsz != 0) {
                 // RELRO technique: .got.plt has been merged into .got and made read-only.
                 // Find .got end from the highest relocation address.
                 void** min_addr = (void**)-1;
-- 
2.47.0

