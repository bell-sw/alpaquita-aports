# Description:

_flavor=lts
pkgname=linux-${_flavor}
pkgver=6.18.40
pkgrel=0
_kernver=${pkgver%.*}
pkgdesc="$VENDOR_DISTRO_NAME Linux lts kernel"
url="https://www.kernel.org"
depends="dracut kmod"
_depends_dev="perl gmp-dev elfutils-dev bash flex bison"
makedepends="
	$_depends_dev
	bc
	bpftool
	diffutils
	elfutils
	findutils
	gawk
	installkernel
	linux-headers
	openssl-dev
	pahole
	python3
	sbsigntool
	sed
	xz
	"
options="!strip !annobin"
_config=${config:-config-lts.${CARCH}}
source="https://cdn.kernel.org/pub/linux/kernel/v${pkgver%%.*}.x/linux-$_kernver.tar.xz
	lts.x86_64.config
	lts.aarch64.config
	lts-64k.aarch64.config
	extramods
	extramods.x86_64
	extramods.aarch64
	x509.genkey
	../shim/bellsoft-uefi-ca.pem
	../shim/bellsoft-uefi-sign-2026.pem

	0100-efi-Add-an-EFI_SECURE_BOOT-flag-to-indicate-secure-b.patch
	0101-efi-Lock-down-the-kernel-if-booted-in-secure-boot-mo.patch
	0102-mtd-phram-slram-Disable-when-the-kernel-is-locked-do.patch
	0103-arm64-add-kernel-config-option-to-lock-down-when-in-.patch
	0104-pci-pme-wakeups.patch
	0105-smpboot-reuse-timer-calibration.patch
	0106-ata-libahci-ignore-staggered-spin-up.patch
	0107-add-a-per-cpu-minimum-high-watermark-an-tune-batch-s.patch
	0108-ACPI-align-slab-for-improved-memory-performance.patch
	"
subpackages="$pkgname-dev:_dev:$CBUILD_ARCH $pkgname-doc"
subpackages="$subpackages $pkgname-extra-modules:_extramods:$CBUILD_ARCH"

_flavors=
for _i in $source; do
	case $_i in
	*.$CARCH.config)
		_f=${_i%."$CARCH".config}
		_flavors="$_flavors $_f"
		[ "linux-$_f" = "$pkgname" ] && continue
		subpackages="$subpackages linux-${_f}:_${_f//-/_}:$CBUILD_ARCH \
			linux-${_f}-dev:_dev:$CBUILD_ARCH \
			linux-${_f}-extra-modules:_extramods:$CBUILD_ARCH"
		;;
	esac
done
builddir="$srcdir"/linux-$_kernver

if [ "${pkgver%.0}" = "$pkgver" ]; then
	# Prepend to apply first
	source="patch-$pkgver.patch.xz::https://cdn.kernel.org/pub/linux/kernel/v${pkgver%%.*}.x/patch-$pkgver.xz $source"
fi
arch="all"
license="GPL-2.0"

prepare() {
	default_prepare

	# remove localversion from patch if any
	rm -f localversion*
}

_kernelarch() {
	local arch="$1"
	case "$arch" in
		aarch64*) arch="arm64" ;;
		arm*) arch="arm" ;;
		ppc*) arch="powerpc" ;;
		s390*) arch="s390" ;;
		riscv*) arch="riscv" ;;
		loongarch64) arch="loongarch" ;;
	esac
	echo "$arch"
}

_prepareconfig() {
	local _flavor="$1"
	local _arch="$2"
	local _config=$_flavor.$_arch.config
	local _builddir="$srcdir"/build-$_flavor.$_arch
	local _cfg_file="$_builddir"/.config
	mkdir -p "$_builddir"
	echo "-$pkgrel-$_flavor" > "$_builddir"/localversion-$VENDOR_DISTRO

	cp "$srcdir"/$_config "$_cfg_file"
	msg "Configuring $_flavor kernel ($_arch)"
	make -C "$builddir" \
		O="$_builddir" \
		ARCH="$(_kernelarch $_arch)" \
		olddefconfig

	mkdir -p "$_builddir"/certs
	cp "$srcdir"/x509.genkey "$_builddir"/certs/

	if [ "$KERNEL_SIGNKEY_X509" ]; then
		cp $KERNEL_SIGNKEY_X509 "$_builddir"/certs/bellsoft_signkey.pem
		scripts/config --file "$_cfg_file" --set-str system_trusted_keys "certs/bellsoft_signkey.pem"
		msg "Using trusted keys: '$KERNEL_SIGNKEY_X509'"
	fi

	if [ "$KERNEL_DEV_BUILD" ]; then
		# increase build speed for development kernels
		scripts/config --file "$_cfg_file" --disable x86_decoder_selftest
	fi
}

listconfigs() {
	for i in $source; do
		case "$i" in
			*.config) echo $i;;
		esac
	done
}

prepareconfigs() {
	for _config in $(listconfigs); do
		local _flavor=${_config%%.*}
		local _arch=${_config%.config}
		_arch=${_arch#*.}
		local _builddir="$srcdir"/build-$_flavor.$_arch
		_prepareconfig "$_flavor" "$_arch"
	done
}

# this is supposed to be run before version is bumped so we can compare
# what new kernel config knobs are introduced
prepareupdate() {
	clean && fetch && unpack && prepare && deps
	prepareconfigs
	rm -r "$builddir"
}

updateconfigs() {
	if ! [ -d "$builddir" ]; then
		deps && fetch && unpack && prepare
	fi
	for _config in ${CONFIGS:-$(listconfigs)}; do
		msg "updating $_config"
		local _flavor=${_config%%.*}
		local _arch=${_config%.config}
		_arch=${_arch#*.}
		local _builddir="$srcdir"/build-$_flavor.$_arch
		mkdir -p "$_builddir"
		echo "-$pkgrel-$_flavor" > "$_builddir"/localversion-$VENDOR_DISTRO
		local actions="listnewconfig oldconfig"
		if ! [ -f "$_builddir"/.config ]; then
			cp "$srcdir"/$_config "$_builddir"/.config
			actions="olddefconfig"
		fi
		env | grep ^CONFIG_ >> "$_builddir"/.config || true
		make -j1 -C "$builddir" \
			O="$_builddir" \
			ARCH="$(_kernelarch $_arch)" \
			$actions savedefconfig

		cp "$_builddir"/defconfig "$startdir"/$_config
	done
}

set_kbuild_timestamp() {
	# KBUILD_BUILD_TIMESTAMP needs to be parsable by busybox date
	export KBUILD_BUILD_TIMESTAMP="$(date '+%Y-%m-%d %H:%M:%S' -u${SOURCE_DATE_EPOCH:+d @$SOURCE_DATE_EPOCH})"
}

build() {
	unset LDFLAGS
	# for some reason these sometimes leak into the kernel build,
	# -Werror=format-security breaks some stuff
	unset CFLAGS CPPFLAGS CXXFLAGS
	set_kbuild_timestamp
	local i
	for i in $_flavors; do
		_prepareconfig "$i" "$CARCH"
	done
	for i in $_flavors; do
		msg "Building $i kernel"
		cd "$srcdir"/build-$i.$CARCH

		make ARCH="$(_kernelarch $CARCH)" \
			CC="${CC:-gcc}" \
			KBUILD_BUILD_VERSION="$((pkgrel + 1 ))-$VENDOR_DISTRO_NAME"

		if grep -q '^CONFIG_DEBUG_INFO_BTF=y' .config; then
			# Generate vmlinux.h
			bpftool btf dump file vmlinux format c > vmlinux.h

			case "$CARCH" in
			aarch64|ppc64le)
				# aarch64 and ppc64le installs the uncompressed vmlinux, which is extremely
				# large with BTF debuginfo unless stripped
				eu-strip --remove-comment vmlinux
				;;
			esac
		fi
	done
}

_move_extra_modules() {
	local _outdir="$1"
	local _abi_release="$2"
	local _buildflavor="$3"
	local _extra_path="${srcdir}/extra_modules_${_buildflavor}"
	local _mods_path="$_outdir/lib/modules/${_abi_release}/kernel"
	local _extramods="$(cat "$srcdir"/extramods)"
	local _extramods_arch="$(test -f "$srcdir"/extramods.$CARCH && cat "$srcdir"/extramods.$CARCH)"
	local _dir_name _mod_path _m _c _src _dst
	local err=0

	for _m in $_extramods $_extramods_arch; do
		_c="${_m:0:1}"

		case "$_c" in
			#) continue;;
			-) _m="${_m:1}";;
		esac

		_dir_name="$(dirname $_m)"
		[ "${_m: -3}" = ".ko" ] && _m="$_m.xz"

		if [ "$_c" = "-" ]; then
			_src="$_extra_path/$_m"
			_dst="$_mods_path/$_dir_name"
		else
			_src="$_mods_path/$_m"
			_dst="$_extra_path/$_dir_name"
		fi

		mkdir -p "$_dst"
		mv -f "$_src" "$_dst" || err=1
	done

	return $err
}

_package() {
	local _buildflavor="$1" _outdir="$2"
	set_kbuild_timestamp

	cd "$srcdir"/build-$_buildflavor.$CARCH
	local _abi_release="$(make -s kernelrelease)"

	sign_list="boot/vmlinuz-${_abi_release}"
	sign_verify="bellsoft-uefi-ca.pem bellsoft-uefi-sign-2026.pem"

	mkdir -p "$_outdir"/boot "$_outdir"/lib/modules

	local _install
	case "$CARCH" in
		arm*|aarch64) _install="install dtbs_install";;
		*) _install=install;;
	esac

	make modules_install $_install \
		ARCH="$(_kernelarch $CARCH)" \
		INSTALL_MOD_PATH="$_outdir" \
		INSTALL_MOD_STRIP=1 \
		INSTALL_PATH="$_outdir"/boot \
		INSTALL_DTBS_PATH="$_outdir/boot/dtbs-$_buildflavor"

	_move_extra_modules $_outdir $_abi_release $_buildflavor

	rm -f "$_outdir"/lib/modules/${_abi_release}/build \
		"$_outdir"/lib/modules/${_abi_release}/source
	rm -rf "$_outdir"/lib/firmware

	install -D -m644 include/config/kernel.release \
		"$_outdir"/usr/share/kernel/$_buildflavor/kernel.release
}

package() {
	install="$pkgname.pre-upgrade $pkgname.post-deinstall $pkgname.post-install $pkgname.post-upgrade"
	_package lts "$pkgdir"

	# copy files for linux-lts-doc sub package
	mkdir -p "$pkgdir"/usr/share/doc
	cp -r "$builddir"/Documentation \
		"$pkgdir"/usr/share/doc/linux-doc-"$pkgver"/
	# remove files that aren't part of the documentation itself
	for nondoc in \
		.gitignore conf.py docutils.conf \
		Kconfig Makefile
	do
		rm "$pkgdir"/usr/share/doc/linux-doc-"$pkgver"/"$nondoc"
	done
	# create /usr/share/doc/linux-doc symlink
	cd "$pkgdir"/usr/share/doc; ln -s linux-doc-"$pkgver" linux-doc

	msg "Checking modules dependencies"
	local _abi_release="$(make -C "$srcdir/build-lts.$CARCH" -s kernelrelease)"

	cd "$pkgdir"/lib/modules/${_abi_release}

	local _mods="$(find kernel -name *.ko.xz)"
	local _d _m _deps _failed=0

	for _m in $_mods; do
		_deps="$(grep "^$_m:" modules.dep | cut -f2 -d: | xargs)"
		for _d in $_deps; do
			if ! echo "$_mods" | grep -q "$_d"; then
				msg "moddep error: $_m -> $_d"
				_failed=1
			fi
		done
	done

	return $_failed
}

_lts_64k() {
	install="$subpkgname.post-deinstall $subpkgname.post-install $subpkgname.post-upgrade"
	_package lts-64k "$subpkgdir"
}

_extramods() {
	local _flavor=$(echo $subpkgname | sed -E 's/(^linux-|-extra-modules$)//g')
	local _abi_release="$(make -C "$srcdir/build-${_flavor}.$CARCH" -s kernelrelease)"
	local _dir="$subpkgdir"/lib/modules/${_abi_release}/kernel

	depends="$depends linux-${_flavor}=$pkgver-r$pkgrel linux-firmware-extra-modules-any"
	pkgdesc="Additional modules for $VENDOR_DISTRO_NAME Linux $_flavor kernel"

	mkdir -p "$_dir"
	cp -r "$srcdir"/extra_modules_${_flavor}/* "$_dir"/
}

_dev() {
	local _flavor=$(echo $subpkgname | sed -E 's/(^linux-|-dev$)//g')
	local _builddir="$srcdir"/build-$_flavor.$CARCH
	local _abi_release="$(make -C "$_builddir" -s kernelrelease)"
	# copy the only the parts that we really need for build 3rd party
	# kernel modules and install those as /usr/src/linux-headers,
	# simlar to what ubuntu does
	#
	# this way you dont need to install the 300-400 kernel sources to
	# build a tiny kernel module
	#
	pkgdesc="Headers and script for third party modules for $_flavor kernel"
	depends="$_depends_dev"
	local dir="$subpkgdir"/usr/src/linux-headers-"$_abi_release"
	set_kbuild_timestamp

	# first we import config, run prepare to set up for building
	# external modules, and create the scripts
	mkdir -p "$dir"
	cp -a "$_builddir"/.config "$_builddir"/localversion-$VENDOR_DISTRO \
		"$dir"/

	install -D -t "$dir"/certs "$_builddir"/certs/signing_key.x509 || :

	# Install vmlinux.h
	if grep -q '^CONFIG_DEBUG_INFO_BTF=y' "$_builddir"/.config; then
		install -Dm644 "$_builddir"/vmlinux.h -t "$dir/"
	fi

	make -C "$builddir" \
		O="$dir" \
		ARCH="$(_kernelarch $CARCH)" \
		prepare modules_prepare scripts

	# remove the stuff that points to real sources. we want 3rd party
	# modules to believe this is the sources
	rm "$dir"/Makefile "$dir"/source

	# copy the needed stuff from real sources
	#
	# this is taken from ubuntu kernel build script
	# http://kernel.ubuntu.com/git/ubuntu/ubuntu-zesty.git/tree/debian/rules.d/3-binary-indep.mk
	cd "$builddir"
	find .  -path './include/*' -prune \
		-o -path './scripts/*' -prune -o -type f \
		\( -name 'Makefile*' -o -name 'Kconfig*' -o -name 'Kbuild*' -o \
		   -name '*.sh' -o -name '*.pl' -o -name '*.lds' -o -name 'Platform' \) \
		-print | cpio -pdm "$dir"

	cp -a scripts include "$dir"

	find "arch/$_karch" -name include -type d -print | while IFS='' read -r folder; do
		find "$folder" -type f
	done | sort -u | cpio -pdm "$dir"

	install -Dm644 "$srcdir"/build-$_flavor.$CARCH/Module.symvers \
		"$dir"/Module.symvers

	# remove unneeded things
	msg "Removing documentation..."
	rm -r "$dir"/Documentation
	find "$dir" -type f -name '*.o' -printf 'Removing %P\n' -delete
	local _karch="$(_kernelarch $CARCH | sed 's/x86_64/x86/')"
	msg "Removing unneeded arch headers..."
	for i in "$dir"/arch/*; do
		if [ "${i##*/}" != "$_karch" ]; then
			echo "  ${i##*/}"
			rm -r "$i"
		fi
	done

	mkdir -p "$subpkgdir"/lib/modules/"$_abi_release"
	ln -sf /usr/src/linux-headers-"$_abi_release" \
		"$subpkgdir"/lib/modules/"$_abi_release"/build
}

sha512sums="
cf265aed22e83bcd8f34d3c0da2a8307654b16338c8be383784632105e21f0d9dc5b73c3732e415230c4164b2b2c9e5915808335e2f10e28207aeed4d773ca56  patch-6.18.40.patch.xz
88599ffdec96d150c1feb9b261ba93bb0301a9d0e1ad6bef7aeab1f5372cbfc57d8b43c7e902bd8f76921d1dbd8189663c142ea869e51d0e2b483b150ee00fe0  linux-6.18.tar.xz
216dd1874bc0dd6b87ffc9bec365a2d57af916cecbc9554a7b31917af20da954f38ca5c373ab6eadcf316a811ac54a0ee344be26709dd7fc46fa26c750073d5a  lts.x86_64.config
7b8175fae35f6e767207a15928cd982ce1b79852442a423eb1d2158b5fd262d524ad5207ab2712dee55574d1c07ca89889dd89110111227043cab91e2f399958  lts.aarch64.config
8ea61cd522506f661417a9c074583859b9495e58e7a317f3d509ba658db1b405edadd6e894c17f879bcdd2173f0f319aa93374632b48f18a97f96eea76ffd8ba  lts-64k.aarch64.config
d3e2b8218b57fa29fbd3234479d23adb194c6eb353902a35a000f2aca1934960dda9b7ac2224398d08148802a43ab5326056c15706ac80b22b8b95cbe5d95b44  extramods
ba44e77280ebff9f364b48aa2b52126941cb73290534390c9d2f866a26a19cc17c6e1e769305ff98ba4186b44fc7e1f3a438d7dae55432ff6cceb49de693d265  extramods.x86_64
ed43c2ac424dd90ecaccf7ba88723a5ccecf2aaee973ffacae5ac6e0ffd5e36e15eb6ba8628b3018c0acd8207379911af326973f362bd97e83db2a8d8017ac3c  extramods.aarch64
8ab1221f07d35b710da7e27d6e443ca303e03b69f29a21d2a8fef236a48e0b95b5521cd1e4adb1046d048a37a9d52989e5a5c55647da6f57f1ba7bb69230ce4f  x509.genkey
304d6a2d2c25af142b2cf2222079ebf59b8174f0825840dfc88e60016e5fcfe12973e0a250b4172ff7ea77cdb014cb5fec1a474965fee97478a6dcd0cbe6bdd3  bellsoft-uefi-ca.pem
221aed90c73b1264bf270f2f5e970fd66e37fb977830b4b6c02219610ebe9685bf85c9d1f48c54837aec46b60c7e8153e3890c713fb3ae55b48b177b315d3623  bellsoft-uefi-sign-2026.pem
3b672bf2d8b5947ffab93b6e5cd13a73772f4b71b66b29c8b6abb3e0f0e8f9feba62d760766629c4263bfbda4167bd912788a14301f568b07910d4f59a311be3  0100-efi-Add-an-EFI_SECURE_BOOT-flag-to-indicate-secure-b.patch
966e14a47a04dd3bbdb6683f3477b20f1f445dd306ec639647f980633b9704ecce37e8beec7915dfe597d7adf6e27bff218c9aef1677b6ea8bbc320d30b01db1  0101-efi-Lock-down-the-kernel-if-booted-in-secure-boot-mo.patch
61f3a4e1c9d28649b7275ba859d20cc4e900055508e2cf3f0244f921e0c0508d0c9f64e83a3a092c51c024304af673fd9b0cd5b800508f481cd66f733777e2b5  0102-mtd-phram-slram-Disable-when-the-kernel-is-locked-do.patch
2744d8500ef8e4073b206b8ad866fd20ee56c102bcbcee283ec361476e42cfb641e77fb94ab61bfffb227b1daeffe22021461d86e08dc781c93e37cb77311a55  0103-arm64-add-kernel-config-option-to-lock-down-when-in-.patch
1f54b1a686ca5e91d64f996e9475b574870f52012ffe2ccbbc933255b748f1f071f3009d5ef491fec7edf012d9c655f846cb4de01980a31b6a03d39b05823962  0104-pci-pme-wakeups.patch
64a63c3ab1a854bbd9ee3bcb45ee38c37c31078f0018d3e8bbd0372aad1e65c002045875e1661ab7cb1fb6d58d7ceabd5217e2f7c0015d820eccb25aef5233c2  0105-smpboot-reuse-timer-calibration.patch
e9648cf6db3ac1795b2a02045257447ac7d50c518119cc91af9ea6d8f1e4a859a8930068719c27fa4e8007b0948192bfaab24063e5e3da3b3b600e4e2af585e3  0106-ata-libahci-ignore-staggered-spin-up.patch
66907b322ff0ed2473114f0f18a5e50bbb595b15ab4ba215716306e949123e3f6da14f02c34d196a0655d71f2420ac3c43d9b9fd828ac487aa3da4b71a7bc879  0107-add-a-per-cpu-minimum-high-watermark-an-tune-batch-s.patch
33739e800b19c50049fdf6f6f8cd4b04d1540ef293ca0efeb6bdb9919b3671c73713ba30520a87524214f7592512bff39d00f6d35b7a1884ce4e03d8914a8dbf  0108-ACPI-align-slab-for-improved-memory-performance.patch
"
