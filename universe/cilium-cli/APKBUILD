pkgname=cilium-cli
pkgver=0.19.0
pkgrel=15
pkgdesc="CLI to install, manage and troubleshoot Kubernetes clusters running Cilium"
url="https://cilium.io/"
arch="all"
license="Apache-2.0"
makedepends="go"
subpackages="
	$pkgname-bash-completion
	$pkgname-fish-completion
	$pkgname-zsh-completion
	"
source="$pkgname-$pkgver.tar.gz::https://github.com/cilium/cilium-cli/archive/refs/tags/v$pkgver.tar.gz
	byteorder-add-s390x-support.patch

	9999-go-mod.patch
	"
# Test files are not provided by the cilium-cli repository, see also:
# https://github.com/cilium/design-cfps/pull/9
options="!check"

export GOCACHE="${GOCACHE:-"$srcdir/go-cache"}"
export GOTMPDIR="${GOTMPDIR:-"$srcdir"}"
export GOMODCACHE="${GOMODCACHE:-"$srcdir/go"}"

# secfixes:
#   0.19.0-r15:
#     - CVE-2026-46600

build() {
	go build -v \
		-ldflags "-X github.com/cilium/cilium/cilium-cli/defaults.CLIVersion=v$pkgver" \
		./cmd/cilium

	for shell in bash fish zsh; do
		./cilium completion $shell > cilium.$shell
	done
}

package() {
	install -Dm755 cilium -t "$pkgdir"/usr/bin/

	install -Dm644 cilium.bash \
		"$pkgdir"/usr/share/bash-completion/completions/cilium
	install -Dm644 cilium.fish \
		"$pkgdir"/usr/share/fish/vendor_completions.d/cilium.fish
	install -Dm644 cilium.zsh \
		"$pkgdir"/usr/share/zsh/site-functions/_cilium
}

sha512sums="
dd70d3ecc2d0663408afb9e6f584c6594f3c381635c2621269ca3870b9680d570a6a3d8fe9f5b69eff5e1687d00e8c25c3d690d4e4cf7c846de43e45149e679a  cilium-cli-0.19.0.tar.gz
872ccb44ac714420f87c476743d7be1b644c3a5c7e2a975d47720dc970c632ce82bb678421b053ebbc219d4a3b1f2c9f37aa74f3733a055b0f8c1dbe15fed23f  byteorder-add-s390x-support.patch
0c0aede4d5e3730dd3441cd5b69d085e84eb0166fda9f3bd3d13725ad90e7dac240de5f8ac8b66b940ae9cc60c907374a8d0de88cc73030dfc0a8d3ae9fe5d06  9999-go-mod.patch
"
