pkgname=calicoctl
pkgver=3.31.3
pkgrel=4
pkgdesc="Optional command line interface (CLI) to manage Calico resources"
url="https://github.com/projectcalico/calico"
arch="all"
license="Apache-2.0"
makedepends="go"
source="$pkgname-$pkgver.tar.gz::https://github.com/projectcalico/calico/archive/refs/tags/v$pkgver.tar.gz

	9999-go-mod.patch
	"
builddir="$srcdir/calico-$pkgver"
options="!check"

# secfixes:
#   3.31.3-r3:
#     - CVE-2026-46600

build() {
	cd calicoctl
	mkdir -p bin

	go build -o bin/calicoctl \
		-ldflags "
		-X github.com/projectcalico/calico/pkg/buildinfo.Version=$pkgver
		-X github.com/projectcalico/calico/pkg/buildinfo.GitRevision=$VENDOR_DISTRO
		" \
		calicoctl/calicoctl.go 
}

package() {
	cd calicoctl
	install -Dm755 -t "$pkgdir"/usr/bin bin/calicoctl
	install -Dm644 -t "$pkgdir"/usr/share/licenses/$pkgname ../LICENSE.md
}

sha512sums="
622acf4902e8e5ea436892056950c200049d215b3e4b70912a39bf890ba37146f1e6843986749ce62e288334c48f6725dd5b1db944c02d1c54afefd8e5ba452c  calicoctl-3.31.3.tar.gz
d157deb31e8f8005ea9e2d76ef546f34e5db60be1a1ff32acb8b7f53f8b31a8132132865b202ee89e3d715010015348fa2da10188a8daa04336a55cb452cf40a  9999-go-mod.patch
"
